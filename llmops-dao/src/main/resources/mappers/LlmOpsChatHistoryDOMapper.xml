<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.LlmopsChatHistoryMapper">
    <resultMap id="BaseResultMap" type="com.example.model.LlmopsChatHistory">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="role" property="role" jdbcType="TINYINT" />
        <result column="tenant" property="tenant" jdbcType="VARCHAR" />
        <result column="content" property="content" jdbcType="OTHER" typeHandler="org.apache.ibatis.type.JsonTypeHandler" />
        <result column="token" property="token" jdbcType="BIGINT" />
        <result column="creator" property="creator" jdbcType="VARCHAR" />
        <result column="operator" property="operator" jdbcType="VARCHAR" />
        <result column="ctime" property="ctime" jdbcType="TIMESTAMP" />
        <result column="utime" property="utime" jdbcType="TIMESTAMP" />
    </resultMap>

    <!-- 基础字段列表 -->
    <sql id="Base_Column_List">
        id, role, tenant, content, token, creator, operator, ctime, utime
    </sql>

    <!-- 查询条件 -->
    <sql id="Example_Where_Clause">
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>

    <!-- 根据主键查询 -->
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Long">
        select
        <include refid="Base_Column_List" />
        from llmops_chat_history
        where id = #{id,jdbcType=BIGINT}
    </select>

    <!-- 根据条件查询 -->
    <select id="selectByExample" resultMap="BaseResultMap" parameterType="com.example.model.LlmopsChatHistoryExample">
        select
        <if test="distinct">
            distinct
        </if>
        <include refid="Base_Column_List" />
        from llmops_chat_history
        <if test="_parameter != null">
            <include refid="Example_Where_Clause" />
        </if>
        <if test="orderByClause != null">
            order by ${orderByClause}
        </if>
        <!-- 分页支持 -->
        <if test="limit != null">
            limit ${limit}
        </if>
        <if test="offset != null">
            offset ${offset}
        </if>
    </select>

    <!-- 插入记录 -->
    <insert id="insert" parameterType="com.example.model.LlmopsChatHistory">
        insert into llmops_chat_history (id, role, tenant, content, token, creator, operator, ctime, utime)
        values (
        #{id,jdbcType=BIGINT},
        #{role,jdbcType=TINYINT},
        #{tenant,jdbcType=VARCHAR},
        #{content,jdbcType=OTHER,typeHandler=org.apache.ibatis.type.JsonTypeHandler},
        #{token,jdbcType=BIGINT},
        #{creator,jdbcType=VARCHAR},
        #{operator,jdbcType=VARCHAR},
        #{ctime,jdbcType=TIMESTAMP},
        #{utime,jdbcType=TIMESTAMP}
        )
    </insert>

    <!-- 根据主键更新 -->
    <update id="updateByPrimaryKey" parameterType="com.example.model.LlmopsChatHistory">
        update llmops_chat_history
        set
        role = #{role,jdbcType=TINYINT},
        tenant = #{tenant,jdbcType=VARCHAR},
        content = #{content,jdbcType=OTHER,typeHandler=org.apache.ibatis.type.JsonTypeHandler},
        token = #{token,jdbcType=BIGINT},
        creator = #{creator,jdbcType=VARCHAR},
        operator = #{operator,jdbcType=VARCHAR},
        ctime = #{ctime,jdbcType=TIMESTAMP},
        utime = #{utime,jdbcType=TIMESTAMP}
        where id = #{id,jdbcType=BIGINT}
    </update>

    <!-- 根据主键删除 -->
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from llmops_chat_history
        where id = #{id,jdbcType=BIGINT}
    </delete>

    <!-- 根据条件删除 -->
    <delete id="deleteByExample" parameterType="com.example.model.LlmopsChatHistoryExample">
        delete from llmops_chat_history
        <if test="_parameter != null">
            <include refid="Example_Where_Clause" />
        </if>
    </delete>
</mapper>